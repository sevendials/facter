image: bradrydzewski/base
env:
  - PATH=${HOME}/.rbenv/versions/${RBENV_VERSION}/bin:$PATH
script:
  - sudo apt-get update
  - sudo apt-get install build-essential cmake libboost-all-dev libssl-dev libblkid-dev libcurl4-openssl-dev
  - cd ${DRONE_BUILD_DIR}
  - cmake .
notify:
  email:
    recipients:
      - christj@gmail.com
